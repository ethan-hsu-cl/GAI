template_path: templates/I2V standard 5col.pptx
comparison_template_path: templates/I2V comparison 5col.pptx
output_directory: /Users/ethanhsu/Desktop/ethan-hsu-cl/GAI/Report
comment_output_directory: 'Optional: Global output directory for all generated images. Can be overridden per task. Default
  is current directory.'
testbed: http://192.168.31.161/external-testbed/genvideo/
schedule:
  start_time: ''
  comment: 'Time format: HH:MM (24-hour format). Leave empty to start immediately'
tasks:
- folder: Media Files/Tests/1003 Genvideo Test
  img_prompt: 'Transform the subject from the uploaded image into a character in the style of Charlie and Lola (children’s
    cartoon). Use thin, sketchy outlines, flat bold colors, childlike proportions, playful hand-drawn charm, and simple paper-cut
    textures. Retain the subject’s original clothing, hairstyle, facial features, accessories, skin tone, pose, and expression,
    but reinterpret them to match the Charlie and Lola world. Simplify clothing into flat, colorful shapes while keeping the
    outfit recognizable. For the background, adapt the original scene into the Charlie and Lola style, with the same whimsical
    flatness and hand-drawn charm. Negative Prompt: No realistic shading, no detailed rendering, no anime or manga style,
    no 3D modeling, no photographic textures, no realism.'
  model: gpt-image-1
  quality: medium
  design_link: https://docs.google.com/presentation/d/1p9tT-p8iXlS5lQ1vdivpCiE2xSE5FWpVPGNTf-En-Po/edit?slide=id.g3658e09c4de_0_203#slide=id.g3658e09c4de_0_203
  source_video_link: ''
  reference_folder: ''
  use_comparison_template: false

# Configuration Reference
comments:
  model: |
    AI model for image generation.
    Options: 'gpt-image-1', 'gemini-2.5-flash-image-preview'
    Default: 'gpt-image-1'
  img_prompt: |
    Text prompt describing the desired image transformation.
    Should detail the style, preservation requirements, and any negative prompts.
  quality: |
    Output image quality level.
    Options: 'low', 'medium', 'high'
    Default: 'low'
    Note: Higher quality takes longer and may cost more.
  folder: |
    Path to task folder. Should contain 'Source' subfolder with input images.
    Output goes to 'Generated_Image' subfolder (created automatically).
  design_link: |
    URL to design reference document (e.g., Google Slides, Figma).
    Used for report generation.
  source_video_link: |
    URL to source files location (e.g., OneDrive folder).
    Used for report generation.
  reference_folder: |
    Optional path to folder containing reference images for comparison.
  use_comparison_template: |
    Set to true to use comparison_template_path (3 placeholders: source, reference, output).
    Set to false to use template_path (2 placeholders: source, output).
  output_directory: |
    Global output directory for all generated PPTX report files.
    Default is current directory.
  schedule: |
    start_time: HH:MM (24-hour format). Leave empty to start immediately.
    Allows delayed start for batch processing.
  folder_structure: |
    Expected structure:
    task_folder/
    ├── Source/
    │   ├── image1.jpg
    │   └── image2.png
    ├── Generated_Image/  (created automatically)
    │   ├── image1_generated.png
    │   └── image2_generated.png
    └── Metadata/  (created automatically)
        ├── image1_metadata.json
        └── image2_metadata.json
