testbed: http://192.168.31.161/external-testbed/kling/
output_directory: /Users/ethanhsu/Desktop/EthanHsu-cl/GAI/Report
template_path: templates/I2V standard 5col.pptx
comparison_template_path: templates/I2V comparison 5col.pptx
default_params:
  prompt: ''
  model: v3
  character_orientation: video
  mode: pro
  keep_original_sound: true
  element_list_str: ''
output:
  output_directory: /Users/ethanhsu/Desktop/EthanHsu-cl/GAI/Report
  group_tasks_by: 0
schedule:
  start_time: ''
  comment: 'Time format: HH:MM (24-hour format). Leave empty to start immediately'
root_design_link: ''
source_video_link: ''

tasks:
- folder: Media Files/Kling Motion/0305 9 Styles
  prompt: ''
  model: v3
  character_orientation: video
  mode: pro
  keep_original_sound: true
  element_list_str: ''
  design_link: ''
  source_video_link: ''
  reference_folder: ''
  use_comparison_template: true


comments:
  folder_structure: |
    Each task folder should have 'Source Image' and 'Source Video' subfolders.
    All images will be cross-matched with all videos
    (e.g., 3 images × 4 videos = 12 total generations).
  prompt: |
    Text prompt describing the desired motion (optional).
  model: |
    Model version for generation.
    Options: 'v2.6', 'v3'
    Default: 'v3'
  character_orientation: |
    Whether the character orientation follows the image or the video.
    Options: 'image', 'video'
    Default: 'video'
  mode: |
    Generation quality mode.
    Options: 'std' (standard, faster), 'pro' (professional, higher quality)
    Default: 'pro'
  keep_original_sound: |
    Whether to keep the original sound from the source video.
    Default: true
  element_list_str: |
    Optional comma-separated element IDs for the API.
    Default: '' (empty)
  output_group_tasks_by: |
    Set to 0 (or omit) to create one report with all tasks.
    Set to N (e.g., 3) to group N tasks into one combined report.
    Example: with 5 tasks and group_tasks_by=2, creates 3 reports (2+2+1).
  folder: |
    Path to task folder containing Source Image and Source Video subfolders.
  design_link: |
    URL to design reference document (e.g., Google Spreadsheet).
    Used for report generation.
  source_video_link: |
    URL to source files location (e.g., OneDrive folder).
    Used for report generation.
  reference_folder: |
    Optional path to folder containing reference images for comparison.
  use_comparison_template: |
    Set to true to use comparison_template_path (3 placeholders).
    Set to false to use template_path (2 placeholders).
  default_params: |
    Global default parameters applied to all tasks.
    Includes: prompt, model, character_orientation, mode, keep_original_sound, element_list_str
    Can be overridden per task.
  schedule: |
    start_time: HH:MM (24-hour format). Leave empty to start immediately.
    Allows delayed start for batch processing.
